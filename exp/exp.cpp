#include <bits/stdc++.h>
using namespace std;
int main(){
//	int b = 1;
//	int& a = b;
//	b = 3;
//	cout << a;
//	string a = "aa", b = "bb";
//	string s = a + b;
//	cout << s;
//	string CSV_PATH = "D:\CLionProjects233\zDB\exp";
//	ofstream file(CSV_PATH);
//	if (file){
//	    file << image_path << "," << label << "," << prediction << "," << probability << "\n";
//	}
//	file.close();
	/*
	int a[2][3] = { 1,2,3,4,5,6 };
       //方法1
	ofstream ofile;
	ofile.open("result.csv", ios::out | ios::trunc);//判断.csv文件是否存在，不存在则建立，每次都会删除之前文件，重新生成，不可复写
    //ofile.open("result.csv", ios::out | ios::app);//所有输出都添加在文件末尾，可复写
	ofile << "First,Second,Third" << endl;
	for (int i = 0; i < 2; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			ofile << a[i][j] << ",";
		}
		ofile << "\n";
	}
	ofile.close();	
	*/
	vector<string> a;
	a.insert(3, "ahh")
//	for(int i = 0; i < 5; i++){
//		a.push_back("");
//	}	
	//a[3] = "233";
	cout << a[3];
	return 0;
}
